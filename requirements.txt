# --- core (always needed)
numpy>=1.26
python-dotenv>=1.0
PyYAML>=6.0
streamlit>=1.36

# --- LLM providers: install AT LEAST ONE
google-genai>=1.0     # Gemini: chat + embeddings, generous free tier
anthropic>=0.40
openai>=1.40          # chat + embeddings (text-embedding-3-small)
# groq>=0.11          # fast hosted chat, and hosted Whisper

# --- transcription: faster-whisper needs no API key and gives exact timestamps
faster-whisper>=1.0

# --- optional: free local embeddings, no API calls
# sentence-transformers>=3.0   # FERMI_EMBED_PROVIDER=local
